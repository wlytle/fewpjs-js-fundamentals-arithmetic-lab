const num1 = 2;
const num2 = 31;

const multiply = num1 * num2;

let random = Math.floor(Math.random() * 100) + 1;

let mod = 24 % 5;

let set = [20, 2, 3, 4, 5, 6, 8, 4, 2];

const max = Math.max(...set);
